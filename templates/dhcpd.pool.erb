#################################
# <%= name network mask %>
#################################
subnet <%= network %> netmask <%= mask %> {
<% if range -%>
  pool
  {
    range <%= range %>;
  }
<% end -%>

  option subnet-mask <%= mask %>;
  option routers <%= gateway %>;
}

